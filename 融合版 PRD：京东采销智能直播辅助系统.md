# JoyCue 产品需求文档（PRD）

> 版本：v3.0 - WIP 对齐版  
> 更新日期：2026-07-14  
> 需求基线：`JoyCue-值得信赖的京东采销AI导播搭档（WIP）.pdf`（51 页，含原型图片）  
> 产品定位：值得信赖的京东采销 AI 导播搭档 / 直播经营中枢智能体

## 1. 产品概述

JoyCue 面向京东采销主播和场控，将商品与供应链事实、直播互动、场控动作和交易结果连接为一套可持续演进的直播经营系统。它不替代真人主播，而是让“最懂货的人”稳定地讲得准、接得快、卖得稳。

产品首期以四个能力钩子进入现有直播工作流：

- JoyCard：可信商品手卡与脚本。
- JoyGuard：价格、库存、履约和合规风险哨兵。
- JoyRadar：高价值评论识别与主播辅助响应。
- JoyClip：按 SKU 沉淀直播回放和切片素材。

## 2. 商业价值与机会洞察

### 2.1 行业判断

直播电商已经从流量扩张进入精细化经营阶段。JoyCue 的机会不在重新创造交易渠道，而在提升现有直播场景的单位流量产出、内容准确性、组织人效和内容复用率。

JoyCue 位于直播经营价值链中游：

```text
上游：商品/价格/库存/履约/售后/评价事实
  -> JoyCue 可信事实与实时经营中枢
  -> 下游：评论响应/点击/加购/成交/退货/内容复用
```

相较数字人、通用大模型、传统中控和真人场控，差异点不是“也能生成内容”，而是可通过 Adapter 接入京东业务事实，并保留人工确认、风险解释和结果归因。

### 2.2 核心用户

| 用户 | 优先级 | 诉求 |
| --- | --- | --- |
| 采销主播 | P0 | 手卡准确简洁、高意图问题不漏、保留真人控制 |
| 场控/运营 | P0 | 减少多系统切换，风险可解释，快速复盘和切片 |
| 品类负责人 | P1 | 增量 GMV、人效、退货和跨直播间复制可衡量 |
| 品牌商家 | P2 | 低门槛、行业模板、权限、合规和 ROI 证明 |

### 2.3 核心痛点

- 播前：SKU 手卡制作慢，规格、价格、库存容易出错。
- 播中：评论刷屏、高意图问题漏看；主播同时讲解、查参数、控节奏，认知负荷高。
- 播后：整场回放难复用，话术、评论、点击和交易之间缺少关联复盘。

目标是把用户从“找信息、盯风险、捞问题”转向“确认与决策”。

### 2.4 商业化方向

先在京东采销直播间验证价值，再将能力标准化为商家可订阅、可计量、可配置的产品：基础版、专业版、直播小时/AI 调用/切片用量包、行业能力包和企业版。首期不以纯 GMV 分成作为主要收费方式。

## 3. 产品范围

### 3.1 本课题 Demo 范围

本仓库只负责前端和后端业务骨架：

- 完成页面、交互、状态和端到端主流程。
- 定义商品、直播、评论、数据中心、切片和算法的稳定 API/Adapter 契约。
- 使用本地 H2 与 Fixture Adapter 保证无公司环境也能运行演示。
- 后续真实数据或算法接入时，替换 Adapter 实现，不改页面主流程和对外 API。

### 3.2 暂不实现

- 真实直播平台 SDK、直播推流、摄像头采集和真实回放文件。
- 交易、库存、价格、售后等生产数据源。
- ASR、意图分类、RAG、脚本模型、诊断模型和视频切片算法本体。
- 生产鉴权、企业权限、计费、SLA、审计和多租户。

以上能力必须保留接口和降级策略，但不在本课题中伪装成已接入。

## 4. 产品信息架构

左侧主导航：

1. 商品管理 - JoyCard 与播前排雷。
2. 直播控制台 - 直播前准备、JoyGuard、JoyRadar 和动态提词。
3. 智能切片 - JoyClip 回放与剪辑工作台。
4. 数据中心 - 经营复盘、内容效果和数据对比。

全局规则：

- 默认进入商品管理。
- 左侧导航默认 240px，支持全页面收起/展开。
- 品牌使用 `JoyCue`，字号大于导航文字并左对齐。
- 未注册页面只能显示占位/错误态，不允许白屏。
- 顶部展示当前模块名、通知入口和用户头像占位。

## 5. 端到端主流程

```text
进入 JoyCue
 -> 商品管理：文本/Excel 导入 SKU
 -> 播前排雷
 -> 生成 JoyCard，展示思考状态
 -> 勾选/编辑话术卡片，实时形成最终脚本
 -> 导出 Word 或导入直播台
 -> 直播前准备：商品、脚本、场景、直播信息
 -> 开始直播
 -> 直播中控：商品切换、提词、指标、风险、评论辅助
 -> 结束直播
 -> 回放进入智能切片列表
 -> 数据中心复盘与导出
```

## 6. 功能需求

### 6.1 全局导航（P0）

| 需求 | 验收标准 | 当前 Demo |
| --- | --- | --- |
| JoyCue 品牌 | Logo 清晰、左对齐 | 已实现 |
| 四模块导航 | 当前页高亮、名称与顶部一致 | 已实现 |
| 导航收折 | 收起后主内容自动变宽，再次点击恢复 | 已实现 |
| 页面降级 | 未知 key 显示占位而非白屏 | 已实现 |

### 6.2 商品管理 / JoyCard（P0）

#### 6.2.1 SKU 录入

- 支持每行一个 SKU 的多行文本。
- 支持 `.xlsx/.xls/.csv` 上传入口，单次上限建议 100 个 SKU。
- 真实文件解析由数据 Adapter 实现；Demo 返回任务 ID 并明确展示接入边界。
- 空输入不能提交，解析失败必须给出可操作错误信息。

#### 6.2.2 播前排雷

- 对商品标题和话术检查极限词/高风险表述。
- 输出 `passed`、命中词、风险类型和改写建议。
- Demo 使用本地规则；生产可替换合规 Adapter。

#### 6.2.3 脚本生成

- 点击生成后按钮进入处理中状态，避免重复提交。
- 展示 SKU 清洗、卖点提取、合规检查、模板匹配等过程。
- 长任务统一采用 `taskId + status` 契约。
- Demo 使用后端模板生成；生产替换脚本算法 Adapter。

#### 6.2.4 话术卡编辑与导入

- 每张卡片包含选择框、标题、可编辑话术和关键词。
- 最终脚本仅拼接已勾选卡片，并随编辑实时更新。
- 无选中话术时禁止导出和导入。
- 支持导出 Word 兼容文档。
- “导入直播台”把脚本和 SKU 写入会话上下文并直接切换至直播控制台。

### 6.3 直播控制台（P0）

#### 6.3.1 直播前准备

- 默认展示“直播前准备”Tab。
- 左侧：商品卡片、整体脚本预览。
- 中间：9:16 竖屏预览，占位时必须标明摄像头/画面 Adapter 未接入。
- 右侧：场景选择、摄像头、直播间名称、时间、简介、保存草稿、开始直播。
- 点击开始直播后保存准备数据并进入直播中控台。

#### 6.3.2 直播中控台

- 顶部操作：提词器投屏、暂停/恢复、结束直播。
- 左侧商品：展示商品与讲解中状态，支持上一/下一商品及边界禁用。
- 中间：动态提词器、实时指标、价格/参数/快捷话术。
- 右侧：直播画面状态和 JoyRadar 评论卡片。
- 评论来源入口：登记直播间 URL，真实平台 Adapter 向 `/live/comments/ingest` 推送统一模型。

#### 6.3.3 JoyGuard 规则

| 类型 | 触发 | UI |
| --- | --- | --- |
| 库存风险 | 库存低于阈值 | 红色告警，建议切品 |
| 逼单时机 | 流量达峰且订单流速持平 | 黄色提示 |
| 价格优势 | 本店价低于可验证竞品价 | 绿色提示；不得直接承诺无法验证的“全网最低” |
| 价格劣势 | 本店价高于竞品 | 黄色提示，转向履约和售后事实 |

Demo 使用 Fixture；生产规则由数据 Adapter + 规则/算法 Adapter 提供。

#### 6.3.4 JoyRadar 评论

- 水贴与高价值问题分层。
- 高价值问题展示问题、建议答案、知识匹配状态和 AI/Fixture 标识。
- 支持确认答案、修改答案、标记口播、隐藏水贴。
- 未命中知识时返回“需主播结合商品详情回答”，不得编造事实。

### 6.4 数据中心（P0）

数据中心按日期/场次展示：

- 核心指标：GMV、订单、客单价、PV/UV、停留、点击/加购/转化、库存健康。
- 流量漏斗：进入 → 点击 → 加购 → 下单 → 支付，标出最大流失。
- 商品销售详情 TOP10：讲解时长、GMV、销量、转化、点击、加购、库存、退货。
- 用户互动：评论量、问题占比、回复率、高频问题、情绪和互动热力。
- 主播表现：有效讲解时长、商品切换、响应速度、话术贡献、违规词。
- 回放与切片：讲品时间线、切片状态和切片效果。
- 数据对比：本场/上一场/品类均值/品类 TOP。
- AI 诊断：卖得最好、流失最高、有效话术、改进点、库存风险。

当前 Demo 用 Fixture 展示布局和数据契约；真实指标计算和诊断算法暂不实现。导出先提供 Excel 兼容 CSV 和文本报告接口，生产 PDF/Excel 模板由文档服务 Adapter 提供。

### 6.5 智能切片 / JoyClip（P1）

#### 6.5.1 回放列表

- 显示总数、生成中、已生成、失败数量。
- 按直播标题搜索；无结果时提供清空条件。
- 卡片展示标题、时间、时长、观看、曝光-观看率、GMV 和状态。
- 悬浮显示“智能切片、去剪辑、删除”。
- 状态：`ready/processing/done/failed`。
- 点击智能切片后通过任务 API 进入 `processing`；视频算法接入后更新最终状态。

#### 6.5.2 智能剪辑工作台

- 左侧：AI 剪辑助手对话，Enter 发送，Shift+Enter 换行。
- 中间：9:16 切片预览。
- 右侧：视频片段/图片素材 Tab，支持移除和清单导出。
- 当前 Demo 只验证交互和任务接口；真实生成、转码、下载和对象存储待接入。

## 7. 接入契约

### 7.1 数据 Adapter

- 商品：SKU、标题、图片、参数、价格、库存、履约、售后。
- 直播：状态、在线人数、互动、回放和商品讲解区间。
- 交易：点击、加购、下单、支付、退款、退货。
- 价格/库存：实时快照、变化事件、数据时间和来源。
- 文档/媒体：Word/PDF/Excel、视频任务、素材 URL。

### 7.2 算法 Adapter

- 合规检测、脚本生成、术语翻译。
- 评论意图/价值识别、问题聚合、RAG 回答。
- ASR 与脚本节点匹配。
- 经营诊断、切片区间识别和素材生成。

所有算法结果必须返回 `requestId`、版本、置信度、证据、耗时和降级状态；Demo 可以返回 `dataMode=FIXTURE_ADAPTER`。

## 8. 非功能需求

- 首屏目标 ≤ 2 秒（试点环境验证）。
- 普通 HTTP 请求默认超时 5 秒；AI/媒体长任务异步化。
- 直播主流程即使算法不可用也可手工操作。
- 所有 AI/Fixture 内容必须显式标注来源。
- 真实答案、价格和库存提示要有数据时间与证据。
- 页面异常不白屏，错误提示可恢复。
- 生产接入必须补鉴权、RBAC、幂等、审计、限流、脱敏和监控。

## 9. 指标与埋点

北极星指标：每千名有效观看用户的 AI 辅助增量 GMV。该指标需 A/B 或统计校准，本 Demo 不计算真实增量。

关键埋点：SKU 导入、商品数据读取、手卡生成/修改/确认、风险识别/推送/处理、评论识别/聚合/推送、提示采纳、点击/加购/下单/支付/退款、讲品区间、切片生成/编辑/发布/转化。

统一字段：`sessionId`、`roomId`、`anchorId`、`skuId`、`timestamp`、`modelVersion`、`experimentGroup`、`requestId`。

## 10. 分期与验收

### 本课题交付

- P0 页面和主流程可运行。
- P1 JoyClip 列表与剪辑工作台可交互。
- 前后端契约、Fixture Adapter 和本地数据库可用。
- 一键启动、停止和状态查询可用。
- 真实数据/算法的接入点在 TRD 和架构文档中可定位。

### 后续生产阶段

1. 0-3 个月：真实 SKU/价格/库存/评论接入，四能力 MVP。
2. 3-6 个月：评论模型、提示采纳和交易归因。
3. 6-12 个月：跨品类模板、真实 JoyClip、多直播间看板。
4. 12 个月以上：商家工作台、计费、行业包、企业权限和 SLA。

